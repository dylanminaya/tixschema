//! The `http_rest` dispatcher for `UploadDocumentClientService`, expanded out of the transport's
//! macro into a module of its own.

use crate::tests::UploadDocumentError;
use crate::upload_document_client_service_schema;

upload_document_client_service_http_rest_dispatcher!();
