//! The `http_rest` dispatcher for `ContentClientService`, expanded out of the transport's macro
//! into a module of its own.

use crate::tests::ContentError;

content_client_service_http_rest_dispatcher!();
