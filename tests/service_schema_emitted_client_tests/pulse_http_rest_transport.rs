//! The `http_rest` dispatcher for `PulseClientService`, expanded out of the transport's macro
//! into a module of its own — the Rust twin the bodyless-empty-message group is measured
//! against.

pulse_client_service_http_rest_dispatcher!();
