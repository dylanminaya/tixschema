//! The `http_rest` dispatcher for `ThumbnailClientService`, expanded out of the transport's macro
//! into a module of its own — the same placement every dispatcher macro in this crate gets.

use crate::tests::ThumbnailError;

thumbnail_client_service_http_rest_dispatcher!();
